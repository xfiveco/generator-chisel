#!/usr/bin/env bash

if [ -f /.dockerenv ]; then
  exec "${@}"
else
  this_file_dir=$(dirname "${BASH_SOURCE[0]}")
  npm run --silent devcontainer -- exec --workspace-folder "${this_file_dir}/.." "${@}"
fi
